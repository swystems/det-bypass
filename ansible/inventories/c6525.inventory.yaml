all:
  hosts:
    xdp1:
      ansible_host: swystems.xdp1
      devs:
        - enp65s0f0np0
      ibdevs:
        - rocep65s0f0
      ip: "10.10.1.1"
    xdp2:
      ansible_host: swystems.xdp2
      devs:
        - enp65s0f0np0
      ibdevs:
        - rocep65s0f0
      ip: "10.10.1.1"

  vars:
    ansible_user: mdr
    isolated_cores: "11"
    cpu_freq: 2400000000 # 2.4 GHz
    base_dir: "/users/{{ ansible_user }}"

tx_nodes:
  hosts:
    xdp1

rx_nodes:
  hosts:
    xdp2